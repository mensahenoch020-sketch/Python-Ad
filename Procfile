# Railway (and other Procfile-aware hosts) run this to start the web app.
# $PORT is supplied by the platform; HTTPS is terminated at the platform edge.
web: uvicorn app:app --host 0.0.0.0 --port $PORT
